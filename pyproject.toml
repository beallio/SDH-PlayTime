[tool.ruff]
target-version = "py311"
exclude = ["decky.pyi", "docs", "tests", "py_modules/tests"]

[tool.ruff.lint]
select = ["E", "F"]

[tool.ty.environment]
python-version = "3.11"

[tool.pytest.ini_options]
testpaths = ["py_modules/tests", "tests"]
